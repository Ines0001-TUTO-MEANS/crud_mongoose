(function(app) {
	app.service('Boom_service', function() {
      
      return {
          get statusCode () {
              return ':' + first_name;
          },
          
      }

	});
})(CrudMongoose);